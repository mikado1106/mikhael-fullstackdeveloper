# Base URL of the backend API, including the /api prefix.
# Leave empty in development: Vite proxies /api to http://localhost:3000.
# On Vercel set it to your Render URL, e.g. https://indokerja-api.onrender.com/api
VITE_API_URL=
